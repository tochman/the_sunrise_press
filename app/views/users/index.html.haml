%h1 Users List
%br
%table
    %tr
        %th Email
        %th Role
        %th Change Role
    - @users.each do |user|
        %tr
            %td= user.email
            %td= user.role
            -if user.role == 'member'
                %td= link_to 'Set as Journalist', user, method: :patch
